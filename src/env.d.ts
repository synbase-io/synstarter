declare namespace App {
    // Change this type to change the data which your `Astro.locals` object holds.
    // It's recommended to use middleware to add data to the `Astro.locals` object, so you can use it within your components or pages.
    // interface Locals {}
}
